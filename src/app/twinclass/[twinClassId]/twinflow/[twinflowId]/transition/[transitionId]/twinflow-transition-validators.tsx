import {TwinFlowTransition} from "@/lib/api/api-types";

export function TwinflowTransitionValidators({transition, onChange}: { transition: TwinFlowTransition, onChange: () => any }) {
    return <></>
}